services:
  - type: web
    name: ltp-analysis-dashboard
    env: docker
    plan: free
    dockerfilePath: Dockerfile
    startCommand: "streamlit run app.py --server.port $PORT --server.address 0.0.0.0 --server.headless true"
    envVars:
      - key: STREAMLIT_SERVER_HEADLESS
        value: "true"
