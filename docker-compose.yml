version: '3.8'
services:
  ltp_dashboard:
    build: .
    container_name: ltp_dashboard
    ports:
      - "8501:8501"
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
    command: streamlit run app.py --server.port 8501 --server.address 0.0.0.0
